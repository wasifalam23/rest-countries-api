import React from 'react';
import styles from './CountryMessage.module.css';

const CountryMessage = () => {
  return <p className={styles.message}>Country not found.</p>;
};

export default CountryMessage;
